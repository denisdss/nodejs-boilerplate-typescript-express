import { ApiModel } from "../models/api-model";


export const findData = async (param?: string): Promise<ApiModel[]> => {
  const data = 
  [
    {
      id:0,
      name:"ok"
    }
  ]
  return data;
};